"use client";

/**
 * Stub d'accueil pour le futur modèle .glb (bibliothèque/scriptorium).
 *
 * Marche à suivre une fois le fichier prêt :
 * 1. Place le .glb dans public/models/scriptorium.glb
 * 2. npx gltfjsx public/models/scriptorium.glb --typescript --transform
 *    → génère un composant avec les bons <mesh>/<meshStandardMaterial>
 * 3. Colle le contenu généré ici (garde le `export default function
 *    ScriptoriumModel`) et remplace le wireframe placeholder dans
 *    MainHall.tsx par <ScriptoriumModel />.
 */
export default function ScriptoriumModel() {
  return null;
}
